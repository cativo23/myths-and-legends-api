meta {
  name: List Users
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/{{api_version}}/users?page=1&size=20&sort=email&order=asc
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
