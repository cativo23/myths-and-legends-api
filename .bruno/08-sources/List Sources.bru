meta {
  name: List Sources
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/{{api_version}}/sources?sort=name&order=asc
}

query {
  source_type: Book
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
