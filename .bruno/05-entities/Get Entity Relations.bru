meta {
  name: Get Entity Relations
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/{{api_version}}/entities/:id/relations
}

params:path {
  id: 1
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
