meta {
  name: List Countries
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/{{api_version}}/countries?sort=name&order=asc
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
}
